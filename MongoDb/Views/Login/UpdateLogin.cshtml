@model MongoDb.Dtos.LoginDto.GetLoginByIdDto
@{
    ViewData["Title"] = "Giriş Bilgilerini Güncelle";
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
}

<div class="col-lg-8 offset-lg-2 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Giriş Bilgilerini Güncelle</h4>
            <p class="card-description text-muted">Yönetici paneli giriş bilgilerini güncellemek için aşağıdaki alanları düzenleyin.</p>
            
            <form method="post" class="forms-sample">
                <input type="hidden" asp-for="LoginId" />
                
                <div class="form-group">
                    <label asp-for="UserName">Kullanıcı Adı</label>
                    <input type="text" class="form-control" asp-for="UserName" placeholder="Kullanıcı adını giriniz" required>
                </div>
                <div class="form-group">
                    <label asp-for="Password">Şifre</label>
                    <input type="password" class="form-control" asp-for="Password" placeholder="Yeni şifreyi giriniz" required>
                </div>
                
                <div class="text-right mt-4">
                    <button type="submit" class="btn btn-primary mr-2">
                        <i class="typcn typcn-tick"></i> Güncelle
                    </button>
                    <a href="/Login/LoginList" class="btn btn-light">İptal</a>
                </div>
            </form>
        </div>
    </div>
</div>
