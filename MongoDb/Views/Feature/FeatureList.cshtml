@model List<MongoDb.Dtos.FeatureDto.ResultFeatureDto>
@{
    ViewData["Title"] = "Özellikler";
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
    int i = 0;
}

<div class="row mb-4">
    <div class="col-md-4 stretch-card grid-margin">
        <div class="card bg-gradient-primary text-white">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="text-white-50 mb-2">Toplam Özellik</h6>
                        <h2 class="mb-0 text-white">@Model.Count</h2>
                    </div>
                    <i class="typcn typcn-star-outline" style="font-size: 48px; opacity: 0.3;"></i>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h4 class="card-title mb-0">Özellikler Listesi</h4>
                <a href="/Feature/CreateFeature/" class="btn btn-inverse-info btn-fw">
                    <i class="typcn typcn-plus"></i> Yeni Özellik Ekle
                </a>
            </div>

            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Başlık</th>
                            <th>Açıklama</th>
                            <th>Görsel</th>
                            <th class="text-center">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            i++;
                            <tr>
                                <td><span class="badge badge-light">@i</span></td>
                                <td><strong>@item.Title</strong></td>
                                <td>@item.Description</td>
                                <td>
                                    <img src="@item.ImageUrl"
                                         width="80"
                                         height="80"
                                         style="object-fit:cover;border-radius:10px;" />
                                </td>
                                <td class="text-center">
                                    <a href="/Feature/UpdateFeature/@item.FeatureId"
                                       class="btn btn-inverse-warning btn-sm"
                                       title="Güncelle">
                                        <i class="typcn typcn-edit"></i>
                                    </a>
                                    <a href="/Feature/DeleteFeature/@item.FeatureId"
                                       class="btn btn-inverse-danger btn-sm"
                                       onclick="return confirm('Silmek istediğinize emin misiniz?')"
                                       title="Sil">
                                        <i class="typcn typcn-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
