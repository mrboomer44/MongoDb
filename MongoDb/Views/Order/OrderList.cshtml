@model List<MongoDb.Dtos.OrderDto.ResultOrderDto>
@{
    ViewData["Title"] = "Siparişler";
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
    int i = 0;
}

<div class="row mb-4">
    <div class="col-md-4 stretch-card grid-margin">
        <div class="card bg-gradient-primary text-white">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="text-white-50 mb-2">Toplam Sipariş</h6>
                        <h2 class="mb-0 text-white">@Model.Count</h2>
                    </div>
                    <i class="typcn typcn-shopping-cart" style="font-size: 48px; opacity: 0.3;"></i>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h4 class="card-title mb-0">Sipariş Listesi</h4>
            </div>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Müşteri</th>
                            <th>E-posta</th>
                            <th>Telefon</th>
                            <th>Ürün</th>
                            <th>Tarih</th>
                            <th class="text-center">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            i++;
                            <tr>
                                <td><span class="badge badge-light">@i</span></td>
                                <td><strong>@item.CustomerName</strong></td>
                                <td>@item.Email</td>
                                <td>@item.Phone</td>
                                <td>@item.ProductName</td>
                                <td>@item.OrderDate.ToString("dd.MM.yyyy")</td>
                                <td class="text-center">
                                    <a href="/Order/OrderDetails/@item.OrderId"
                                       class="btn btn-inverse-info btn-sm" title="Detay">
                                        <i class="typcn typcn-eye"></i>
                                    </a>
                                    <a href="/Order/DeleteOrder/@item.OrderId"
                                       class="btn btn-inverse-danger btn-sm"
                                       onclick="return confirm('Siparişi silmek istediğinize emin misiniz?')"
                                       title="Sil">
                                        <i class="typcn typcn-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>